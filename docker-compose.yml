
networks:
  app-network:
    external: true

services:
  web: 
    build: .
    volumes:
      - ./:/usr/src 
    working_dir: microblog/
    command: sh -c 'alembic upgrade head && python -m src'
    networks:
      - app-network
    ports:
      - 8000:8000
    env_file:
      - .env
    depends_on:
      - database

  database:
    image: postgres:latest
    container_name: postgres-container
    restart: always
    volumes:
      - .postgres:/var/lib/postgres/data
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASS}
      - POSTGRES_DB=${DB_NAME}
    networks:
      - app-network
    ports:
      - '25432:5432'